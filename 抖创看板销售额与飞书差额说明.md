# 抖创看板销售额与飞书差额说明

本文说明 `/creator` 看板「区间销售额」与飞书抖店表汇总不一致的原因（典型场景：看板约 **¥154,674**，飞书约 **¥154,540**，差额约 **¥134**）。

## 两套统计口径

| | 看板（本系统） | 飞书抖店表 |
|---|----------------|------------|
| **先筛谁** | 抖创表：制作团队 ∈ 筛选团队（如 DP视频、冯杰） | 抖店表：每行 **`发布团队`** 属于筛选团队 |
| **怎么关联成交** | 用这些作品的 **作品名**（去空白）匹配抖店 `作品名`，汇总 `增加销售额` | 直接对满足条件的抖店行求和 |
| **成交日期** | 只统计 `日期` 落在筛选日期区间内 | 同上（视图/公式按区间过滤） |

看板实现位置：

- 作品集合：`salesScopeItems`（店铺/体裁/状态/制作团队/关键词，**不含发布日期**）
- 指标：`sumShopSalesEntriesForItems(salesScopeItems, salesDateRange)`（`app/(main)/creator/page.tsx`）
- 入库匹配：`lib/creator/insights.ts` 中 `buildWorkMatchKey` **仅按作品名**，`shopSalesEntries` 挂到抖创作品

飞书侧「发布团队」为抖店表 **链接字段**（非纯文本），需解析选项 ID 才能得到「DP视频」「冯杰」等名称；按该字段汇总时，与参考值 **¥154,540** 一致。

## 为什么感觉「我们筛得更严」，金额却更大？

「更严」作用在 **不同维度**，不是同一条流水线上的叠加：

- **本系统更严在**：这笔成交对应的作品，必须在抖创里存在，且 **制作团队** 属于筛选团队。
- **飞书更严在**：这笔成交在抖店行上，**发布团队** 字段必须标为对应团队。

交叉之后会出现：

- 抖创认团队、作品名能匹配，但抖店 **发布团队为空** → **只有看板计入**（多计）
- 抖店 **发布团队** 已是 DP视频/冯杰，但作品名与抖创标题不完全一致 → **只有飞书计入**（少计）

因此不是「整体更严所以一定更小」，而是两边各严一块，净差额可正可负。

## 差额构成（DP视频 + 冯杰，2026-05-01 ~ 2026-05-28 示例）

对账结果（与当时 Mongo + 飞书 API 一致）：

```
看板多计（发布团队为空，但抖创制作团队匹配、作品名能挂上）  +¥330.6
看板少计（发布团队为 DP视频/冯杰，但作品名与抖创标题不一致）  −¥196.7
────────────────────────────────────────────────────────────
净差额 ≈ +¥134  →  154,674 − 154,540
```

### 看板多计示例（+¥330.6）

| 成交日期 | 金额 | 说明 |
|----------|------|------|
| 2026-05-21 | ¥263.7 | 澳诺相关作品；抖创 **制作团队 = DP视频**，抖店 **`发布团队` 为空** |
| 2026-05-21 | ¥66.9 | 同上标题的另一条抖店成交 |

看板因 **作品名匹配** 仍计入；飞书因 **发布团队** 未标团队而不计入。

### 看板少计示例（−¥196.7）

抖店行 **`发布团队 = DP视频`**，但 **作品名** 与抖创入库标题存在差异（如 `#` 后空格、标点不一致），作品名键对不上，`shopSalesEntries` 为空或挂错键，区间汇总漏计。飞书仍按抖店行自身 **发布团队** 计入。

## 已排除的原因

- **日期边界**：5/28 当日无额外团队相关成交，改结束日不影响总额。
- **同日多行合并**：抖店索引按「作品名 + 成交日」合并后再汇总，与逐行相加结果一致（无重复加总）。
- **跨团队同名作品**：Mongo 中未发现「同一作品名分属 DP视频/冯杰 与其他团队」导致的重复组。
- **无作品名跳过**：无作品名正向金额约 ¥26，不足以解释 ¥134 净差额。

## 与看板其他指标的区别

| 指标 | 口径 |
|------|------|
| **区间销售额**（卡片） | 抖店成交 + `salesScopeItems` + 作品名去重 + 成交日期区间 |
| **日期内发布作品销售额**（卡片） | 同上，但作品集合为 `filteredItems`（含 **发布日期** 在区间内） |
| **表格「累积销售额」** | 抖创表字段 `salesAmount`（发布以来累计），**非**抖店明细 |
| **表格「区间销售额」列** | 该行 `shopSalesEntries` 在日期区间内合计（**按行**，不去重） |

## 若要与飞书完全一致

可选方向：

1. **对齐飞书（推荐）**  
   区间销售额改为：只统计抖店表中 **`发布团队`**（解析链接/选项后）属于当前筛选团队的行，并按成交日期区间求和。入库时可同时保留 `shopSalesEntries` 供表格展示，但卡片汇总走「按发布团队」逻辑。

2. **坚持作品名匹配**  
   接受与飞书存在净差额；并加强 **标题归一化**（`#` 前后空格、全角等），减少「飞书有、看板无」的约 ¥196.7。

3. **数据修复**  
   在飞书抖店表补全 **`发布团队`**（如澳诺空团队行），可减少「看板有、飞书无」部分。

---

*文档根据 2026-05-28 前后 Mongo + 飞书 API 对账结论整理。*
